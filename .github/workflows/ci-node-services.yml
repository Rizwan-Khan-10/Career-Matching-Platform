name: CI - Node Services

on:
  push:
    paths: ['apps/*-service/**', 'apps/websocket-gateway/**']
  pull_request:
    paths: ['apps/*-service/**', 'apps/websocket-gateway/**']

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      DATABASE_URL: "postgresql://placeholder:placeholder@localhost:5432/placeholder"
    strategy:
      matrix:
        service:
          - auth-service
          - user-service
          - resume-service
          - job-service
          - matching-results-service
          - notification-service
          - websocket-gateway
          - chat-service
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'
          cache-dependency-path: pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
      - run: |
          if [ -f "apps/${{ matrix.service }}/prisma/schema.prisma" ]; then
            pnpm --filter ${{ matrix.service }} exec prisma generate
          fi
      - run: pnpm --filter ${{ matrix.service }} run build